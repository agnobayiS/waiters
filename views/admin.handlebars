<body>


    <form action="/admin" method="get">

        <div class="containe">

            {{#if messages.erro}}
            <div style="height:30px ;" class="massages">
                <h3 style="color:red; font-size:15px " class="massages">{{messages.erro}}</h3>
            </div>{{/if}}


        </div>
        <div class="containe">
            <div class="row">
                <div class="table ">

                        {{#each data}}
                    <table>
                        <tr>
                            <th>{{this.day}}</th>
                        </tr>
                        <tr>
                            <td style="color: white ;"><a href="./days/:name">{{this.waiter}}</a></td>
                        </tr>
                    </table>
                         {{/each}}   
                        


                </div>

            </div>
        </div>
    </form>
    {{#each data}}

    <form action="/resert/{{this.names_id}}" method="get">
        {{/each}}

        <div style="padding-top: 40px ;">
            <button class="btn" type="resert">RESERT DATA</button>
        </div>
    </form>

</body>